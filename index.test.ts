import { describe, expect, test, beforeAll } from "bun:test";

beforeAll(() => {
  // Setup Test
});

// Test
describe("Math", () => {
  // Addition
  test("Addition", () => {
    expect(2 + 2).toBe(4);
  });
});
